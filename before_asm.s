

## **Step 1: কম্পিউটার আর্কিটেকচার ও হার্ডওয়্যার বোঝা**

1. CPU কি, কিভাবে কাজ করে (Control Unit, ALU, Registers)।
2. Memory hierarchy: Registers, Cache, RAM, ROM।
3. Bus, I/O devices, Interrupts এবং তাদের কাজ।
4. Instruction cycle: Fetch → Decode → Execute → Write-back।
5. Endianness: Little-endian vs Big-endian।

---

## **Step 2: Number System & Binary Logic**

6. Binary, Octal, Decimal, Hexadecimal সংখ্যা চেনা।
7. Binary addition, subtraction, multiplication, division।
8. Two’s complement, One’s complement, Signed/Unsigned numbers।
9. Bitwise operations: AND, OR, XOR, NOT, shifts।
10. Logic gates: AND, OR, NOT, NAND, NOR, XOR, XNOR।

---

## **Step 3: Data Representation & Memory**

11. Bits, Bytes, Words, Double Words, Quad Words।
12. ASCII, Unicode, UTF-8, UTF-16 character representation।
13. Floating point numbers (IEEE 754 standard)।
14. Memory addressing: Physical vs Virtual।
15. Stack & Heap difference, pointers, memory layout।

---

## **Step 4: Programming Basics**

16. Variables, constants, data types (int, float, char, etc)।
17. Operators: arithmetic, relational, logical, bitwise।
18. Control structures: if, else, switch, loops।
19. Functions: definition, parameters, return values।
20. Scope & Lifetime of variables।

---

## **Step 5: Low-level Data Manipulation**

21. Arrays, structs, records।
22. Pointers, pointer arithmetic।
23. Memory alignment & padding।
24. End-of-line and null terminator concept।
25. Type casting & type conversion।

---

## **Step 6: Tools & Environment**

26. Command line interface (CLI) usage।
27. Text editors (vim, nano, VSCode, helix) proficiency।
28. Compiler basics (gcc, clang) and flags।
29. Debuggers (gdb, lldb) ব্যবহার।
30. Version control (git) ও basic commands।

---

## **Step 7: Algorithmic Thinking**

31. Basic sorting & searching algorithms।
32. Recursion & iterative methods।
33. Algorithm complexity: Big O notation।
34. Stack & Queue simulation।
35. Simple problem-solving in pseudo-code।

---

## **Step 8: Operating System Concepts**

36. Processes & threads, scheduling।
37. System calls, file I/O, streams।
38. Memory management: paging, segmentation।
39. Interrupt handling & signals।
40. Kernel vs User mode।

---

## **Step 9: Networking & Communication (Optional but Useful)**

41. TCP/IP basics, sockets।
42. Ports, protocols, and packet structure।
43. Client-server communication।
44. Simple request-response logic।
45. Network byte order & conversion।

---

## **Step 10: Debugging & Deep Understanding**

46. Reading hex dumps & binary files।
47. Using printf/debug prints to trace program।
48. Understanding compiler output & assembly translation।
49. Writing small C programs & predicting memory/register effect।
50. Mental model of CPU execution, pipeline, hazards, and dependencies।

---

